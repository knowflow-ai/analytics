"""Open-source edition shell.

Everything in this package composes the shared analytics core for a standalone,
single-user deployment with no RAGFlow: OpenAI-compatible model gateways, a
file-backed settings store and a thin HTTP shell that serves the bundled web UI.
The core modules are consumed as-is and never modified from here.
"""
